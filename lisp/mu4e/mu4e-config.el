;; auto-generated

(defconst mu4e-mu-version "1.10.5"
  "Required mu binary version; mu4e's version must agree with this.")

(defconst mu4e-builddir "/tmp/guix-build-mu-1.10.5.drv-0/build/mu4e"
  "Top-level build directory.")

(defconst mu4e-doc-dir "/gnu/store/1kdlxl9cr2d8v9x51kgg2z4i8gxwc889-mu-1.10.5/share/doc/mu"
  "Mu4e's data-dir.")

(provide 'mu4e-config)
